// combine these two using spread

const firstArr = "These are happy times".split(" ")
const secondArr = "O my brother.".split(" ")

// create a new cat with a different name using spred with this object but don't add any keys

const krusty = {
  originalName: 'Ginger',
  species: 'orange calico',
  cool: true,
};

// now copy the object you made an add a key using spread

const kuldee = {
  originalName: 'Marigold',
  species: 'orange calico',
  cool: true,
  collar: false
}

// pull out originalName and leave the rest of the object intact using destructuring

const arr1 = "First Second".split(" ")

// put out First and Second into two different variables using destructuring
