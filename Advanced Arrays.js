
// print each number multiplied by two
const num1 = [1, 2, 3, 4, 5];


// Given an array of names, produce a new array that contains each name with "Hello, " prefixed.
const names = ['Alice', 'Bob', 'Charlie'];


// Given an array of ages, create a new array containing only the ages that are 18 and above.
const ages = [15, 20, 14, 25, 17];

// Check if there's at least one number in the array that is greater than 10.
const num2 = [5, 7, 2, 12, 3];


// Check if all elements in an array are even numbers.
const num3 = [2, 4, 6, 8, 10];


// Find the first number in an array that is divisible by 5.
const num4 = [3, 7, 10, 25, 8];


// Calculate the sum of all numbers in an array.
const num5 = [1, 2, 3, 4, 5];









// // print each number multiplied by two
// const num1 = [1, 2, 3, 4, 5];
// num1.forEach((item) => console.log(item * 2));

// // Given an array of names, produce a new array that contains each name with "Hello, " prefixed.
// const names = ['Alice', 'Bob', 'Charlie'];
// names.map((item) => { return "Hello, " + item});

// // Given an array of ages, create a new array containing only the ages that are 18 and above.
// const ages = [15, 20, 14, 25, 17];
// ages.filter((item) => item >= 18);

// // Check if there's at least one number in the array that is greater than 10.
// const num2 = [5, 7, 2, 12, 3];
// num2.some((item) => item > 10);

// // Check if all elements in an array are even numbers.
// const num3 = [2, 4, 6, 8, 10];
// num3.every((item) => item % 2 === 0);

// // Find the first number in an array that is divisible by 5.
// const num4 = [3, 7, 10, 25, 8];
// num4.find((item) => item % 5 === 0);

// // Calculate the sum of all numbers in an array.
// const num5 = [1, 2, 3, 4, 5];
// num5.reduce((base, acc) => {
//   return base + acc; 
// },0);
